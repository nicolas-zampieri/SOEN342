@startuml setCriteria
title Internal Operation — Query Building

actor User as U
participant "RailSearchApp" as App
participant "Query" as Q
participant "DayUtils" as DU
participant "TimeUtils" as TU

U -> App : handleSearch()
App -> Q : fromForm()

activate Q
Q -> Q : Read form inputs (from, to, traintype, days, etc.)
Q -> DU : parseDaysOfOperation(daysFilterRaw)
DU --> Q : days[]
Q -> TU : parseTimeToMin(depFrom)
TU --> Q : dep_from (minutes)
Q -> TU : parseTimeToMin(depTo)
TU --> Q : dep_to (minutes)
Q --> App : query
deactivate Q

@enduml
